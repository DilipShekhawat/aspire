## Development Process

```bash
> $ cp .env.example .env

> $ composer install

> Update .env file :
    set DB_CONNECTION
    set DB_DATABASE
    set DB_USERNAME
    set DB_PASSWORD

> $ php artisan passport:install

> $ php artisan migrate
```
